package no.unit.nva.datacite;

import no.unit.nva.datacite.model.Creator;
import no.unit.nva.datacite.model.Description;
import no.unit.nva.datacite.model.Identifier;
import no.unit.nva.datacite.model.Resource;
import no.unit.nva.datacite.model.ResourceType;
import no.unit.nva.datacite.model.Title;
import org.apache.http.client.methods.CloseableHttpResponse;
import org.apache.http.util.EntityUtils;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;

import java.io.IOException;
import java.net.URISyntaxException;

import static java.lang.System.out;

public class DataCiteMdsConnectionTest {

    @Before
    public void setUp() throws Exception {
    }

    @After
    public void tearDown() throws Exception {
    }

}